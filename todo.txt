- random error when the server just starts and client reloads doing a public private toggle will usually throw an error
- socket gets out of sync making it so that sometimes when a tweet is privated it will show up and when it is publicised it will disapear